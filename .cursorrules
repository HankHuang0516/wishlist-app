
# Wishlist.ai - Agent Rules & Documentation Guide

## 0. Pre-Work Requirements (必讀)
- **ALWAYS read this file first** before starting any work.
- Based on the task type, read the corresponding documentation files listed below.

---

## 📚 Documentation Reference Guide

### 依據任務類型選擇讀取的文件：

| 任務類型 | 必讀文件 |
|----------|----------|
| **一般開發/功能開發** | `server/src/data/FAQ.md` |
| **進版/部署驗證** | `REGRESSION_TEST_PLAN.md` |
| **安全審計** | `security/audit/Security-Audit-Prompt_Traditional-Chinese.md` |
| **安全指南** | `security/guidelines/Security-Guidelines_Traditional-Chinese.md` |
| **安全指南說明** | `security/guidelines/Security-Guidelines-Explanation_Traditional-Chinese.md` |
| **安全修復** | `security-fixes.md` |
| **前端開發** | `client/README.md` |

### 文件路徑速查：

```
wishlist-app/
├── .cursorrules                    # 🔴 Agent 規則 (必讀)
├── security-fixes.md               # 安全修復紀錄
├── client/
│   └── README.md                   # 前端說明
├── server/
│   └── src/data/
│       └── FAQ.md                  # ⭐ 產品 FAQ (一般任務必讀)
└── security/
    ├── audit/
    │   └── Security-Audit-Prompt_*.md    # 安全審計提示 (16 語言)
    └── guidelines/
        ├── Security-Guidelines_*.md       # 安全指南 (16 語言)
        └── Security-Guidelines-Explanation_*.md  # 指南說明 (16 語言)
```

---

## 🔒 Security & Operational Rules

### 1. Terminal Command Execution
- **NEVER chain commands** using `;` or `&&` (e.g., `git add . ; git commit`).
- ALWAYS execute commands sequentially in separate steps.
- **CRITICAL: FORBIDDEN CHARACTERS (Security Trigger)**
  - **These characters trigger manual permission prompts EVEN IF `SafeToAutoRun` is true.**
  - **NEVER** use them in ANY command argument (especially `git commit` messages):
    - `(` or `)` (Parentheses) -> ❌ `Fix (CSP)` ✅ `Fix CSP`
    - **Conventional Commits Warning**:
      - ❌ `fix(crawler):` -> Triggers Prompt (Parentheses)
      - ✅ `fix: crawler -` -> Safe (No Parentheses)
    - `&` (Ampersand) -> ❌ `UI & API` ✅ `UI and API`
    - `|` (Pipe) -> Remove
    - `;` (Semicolon) -> Remove
    - `$` (Dollar Sign) -> Remove
  - **Reason**: PowerShell treats these as executable blocks/operators. The IDE's security layer intercepts them before the agent's "Safe" flag can apply.
  
  - **Reason**: PowerShell treats these as executable blocks/operators. The IDE's security layer intercepts them before the agent's "Safe" flag can apply.
  
  - ❌ `git commit -m "fix(crawler): update Momo AI info (v0.0.87)"` -> **Triggers Prompt (parentheses)!**
  - ✅ `git commit -m "fix: crawler - update Momo AI info - v0.0.87"` -> **Auto-runs**

  - **Avoid `curl` with complex URLs**:
    - URLs with `?` or `&` often trigger paranoid shell checks even when quoted.
    - ❌ `curl -I "https://example.com?foo=bar&baz=1"`
    - ✅ Use `read_url_content` tool OR `node -e` scripts instead.

- **GLOBAL INSTALL REQUIRES PERMISSION** (設計如此):
  - `npm install -g` 會修改系統全域狀態，因此**永遠需要權限確認**。
  - 這是安全機制，不是 Bug。即使 `npm` 在 Allow List 中也一樣。
  - ❌ 無法自動執行: `npm install -g @railway/cli`
  - ✅ 可以自動執行: `npm install express` (專案內安裝)

### 2. Deployment
- **部署前必須執行 `npm run test -- --run` 確認所有測試通過。**
- **每次部署都必須更新 `client/package.json` 版本號**（使用 `git rev-list --count HEAD` + 1）。
- **手動觸發部署**：push 完成後，再執行一次空 commit 來確保 Railway 開始部署：
  ```
  git commit --allow-empty -m "chore: Trigger redeploy"
  git push
  ```
- The Git "Always Allow" rule is configured for `git` prefix.
- **Git commit messages**: 避免在 `-m` 訊息中使用特殊字元（如 `$`, `()`, `&`, `+`），因為 PowerShell 可能會將其解析為變數或運算符，導致需要權限確認。
  - ❌ `git commit -m "feat: Add 25+ currencies"`
  - ✅ `git commit -m "feat: Add 25 currencies support"`

### 3. Environment
- Use `IPv4` first resolution for Node to avoid Railway/Gmail blocks.
- Trim whitespace from all Environment Variables.

### 4. PowerShell Pipeline Commands
- 即使單獨的指令在 Allow List 中（如 `npm`, `Select-String`, `Select-Object`），**管道組合指令** 仍可能需要手動確認。
- 這是因為 PowerShell 的 token 匹配機制對管道 (`|`) 組合較為謹慎。
- **建議**：直接執行純 `npm` 指令，不用管道過濾，以確保 Allow List 正常運作。

### 5. Railway CLI Configuration
- **登入帳號**: `hankhuang0516@gmail.com`
- **專案名稱**: `thorough-presence`
- **環境**: `production`
- **Authorized Commands** (已設定 Allow List):
  - `railway status` - 查看專案狀態
  - `railway logs` - 查看即時日誌
  - `railway variables` - 查看環境變數
  - `railway up` - 部署
- **Admin API Endpoints** (無需 CLI):
  - Health: `https://wishlist-app-production.up.railway.app/api/admin/health`
  - Stats: `https://wishlist-app-production.up.railway.app/api/admin/stats?key=wishlist-admin-2026`
  - Logs: `https://wishlist-app-production.up.railway.app/api/admin/crawler-logs?key=wishlist-admin-2026`

---

## 🌐 Multi-Language Support

安全相關文件支援以下語言：
- Arabic (阿拉伯文)
- Dutch (荷蘭文)
- English (英文)
- French (法文)
- German (德文)
- Italian (義大利文)
- Japanese (日文)
- Korean (韓文)
- Portuguese (葡萄牙文)
- Russian (俄文)
- Simplified-Chinese (簡體中文)
- Spanish (西班牙文)
- Thai (泰文)
- **Traditional-Chinese (繁體中文)** ← 預設
- Vietnamese (越南文)

---

## 🐛 Known Issues / 已知問題

### Issue D10: Railway Ephemeral Filesystem (2026-01-03)
- **問題**：下載圖片到本地 `/public/uploads/` 後，每次部署圖片都會消失
- **原因**：Railway 使用 ephemeral (暫時性) 檔案系統，容器重建後所有非 Git 檔案都會被清除
- **解決方案**：Option C - 直接使用 Gemini 返回的原始圖片 URL，不下載到本地
- **風險**：某些網站可能阻擋 hotlinking (403 錯誤)
- **未來改進**：考慮使用 Cloudinary 做持久化圖片儲存

### Issue D9: Image Download User-Agent (Previous)
- **問題**：某些電商網站阻擋伺服器下載圖片
- **解決方案**：使用隨機 User-Agent headers (`getRandomHeaders()`)


### Issue D11: CSP Blocking External Images (2026-01-03)
- **問題**：Gemini Fallback 使用 ui-avatars.com 或直接連結外部圖片時，圖片無法顯示
- **錯誤訊息**：`Refused to load the image ... because it violates the following Content Security Policy directive: "img-src 'self' data:"`
- **原因**：Helmet 預設 CSP 嚴格限制 `img-src` 僅允許 `self` 和 `data:`
- **解決方案**：在 `server/src/index.ts` 的 `helmet()` 設定中，明確開放 `img-src` 包含 `https:`, `http:`, `*`
- **預防**：未來新增外部資源引用時，需同步檢查 Helmet CSP 設定

### Issue D12: Google Image Search Fallback (2026-01-03)
- **問題**：商品圖片顯示為 ui-avatars 文字圖 ("Switch 2...") 而非真實產品圖
- **原因**：AI 嘗試使用 `searchGoogleImage` 尋找圖片失敗，系統自動 Fallback 到 ui-avatars
- **可能原因**：
  1. **Quota Exceeded**: Google Custom Search API 每日免費額度僅 100 次
  2. **Missing Keys**: `GOOGLE_API_KEY` 或 `GOOGLE_CSE_ID` 未設定或無效
  3. **No Results**: 搜尋關鍵字找不到相關圖片
- **檢查方式**：查看 `railway logs` 搜尋 "Google Image Search failed"

### Issue D13: Email Sender Configuration (2026-02-01)
- **問題**：修改程式碼中的 `DEFAULT_FROM_EMAIL` 無法立即改變寄件人名稱
- **原因**：Resend SDK 或編譯快取可能導致程式碼變更未即時生效，或者環境變數優先級問題
- **解決方案**：**使用 Railway 環境變數強制覆寫**
  - 設定 `EMAIL_FROM` (例如: `support@twopiggyhavefun.uk`)
  - 這比修改程式碼更可靠且不需要重新部署代碼即可生效 (Railway 會自動 Redeploy)
